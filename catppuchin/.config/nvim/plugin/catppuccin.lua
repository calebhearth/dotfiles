require("catppuccin").setup({
  integrations = {
    gitsigns = true,
    native_lsp = true,
    notify = true,
    semantic_tokens = true,
    treesitter = true,
    treesitter_context = true,
  }
});
