#!/bin/bash

branch=`git branch 2>/dev/null| grep -e ^* | tr -d '* '`
# Get a log of all commits that are in $branch but not on edge
commits=`git pretty origin/master..HEAD`

result="h3. Branch $branch \n"
result="$result
$commits"
echo -e "$result" | less

