#!/usr/bin/ruby
args = {
  '-a' => '--all',
  '-b' => '--branches',
  '-r' => '--remotes',
}

ARGV.reject{ |arg| !args.has_key? arg }

ARGV.map{ |arg| args[arg] }
system "git-forest --sha --style=2 --topo-order #{ARGV.join(' ')} | less -SR"